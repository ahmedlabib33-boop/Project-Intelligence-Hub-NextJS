# The 10X engine itself (vendor/*.whl) is never pip-installed — it's a
# pure-Python wheel loaded directly via sys.path (zipimport), the same way
# both RUN_LOCAL.bat and this Vercel Python function load it (see
# api/schedule_intelligence.py). Only its own runtime dependencies, from the
# wheel's own METADATA, are installed here.
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
python-multipart>=0.0.9,<1
httpx>=0.27,<1
openpyxl>=3.1,<4
python-docx>=1.1,<2
reportlab>=4.2,<5
pypdf>=5,<7
python-pptx>=1,<2
