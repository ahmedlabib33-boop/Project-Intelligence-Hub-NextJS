# Python AI Programming by Eng. Ahmed Labib
# Universal Project Report Engine 26+ — Web + Governed ML Edition

# Document / report processing
pypdf>=5.0.0
pdfplumber>=0.11.0
python-docx>=1.1.0
openpyxl>=3.1.0
python-pptx>=1.0.0
cairosvg>=2.7.0
Pillow>=10.0.0
reportlab>=4.0.0

# Core scientific / ML runtime (required)
numpy>=2.0.0
pandas>=2.2.0
scipy>=1.13.0
joblib>=1.4.0
pyarrow>=18.0.0
scikit-learn>=1.6.0,<2.0

# Competing ML backends (recommended; engine degrades gracefully if an optional backend is unavailable)
xgboost>=2.1.0
lightgbm>=4.3.0
catboost>=1.2.7
torch>=2.6.0

# Drop-in website/API module
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
python-multipart>=0.0.20
httpx>=0.28.0

# Metrics generated by the engine (not pip packages):
# Accuracy, Precision, Recall, F1, Balanced Accuracy, Confidence,
# Confusion Matrix, OOD Score / OOD Rate, Drift Score, MAE, RMSE, R2,
# cross-validation mean/std and model/framework provenance.

# Multi-LLM advisory / provider SDKs (HTTP adapter works through httpx; SDKs retained for compatible deployments)
anthropic>=0.40.0
openai>=1.0.0

# Optional optimization / explainability extensions
optuna>=4.0.0
shap>=0.46.0
